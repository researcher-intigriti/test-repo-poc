pull_request_rules:
  - name: forge-approval-as-B
    conditions:
      - -closed
    actions:
      review:
        type: APPROVE
        message: "LGTM (forged approval via bot_account)"
        bot_account: youssef123ui
